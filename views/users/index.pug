extends /layouts/application.pug

block content
  table.table.table-striped(style="font-family: 'Arbutus Slab', serif;")
    thead
      tr
        th Full Name
        th Email
        th Created At
    tbody
      each user in users
        tr
          td= user.fullName
          td= user.email
          td= user.createdAt